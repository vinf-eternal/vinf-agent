# 项目变项配置（project/agents.md）
#
# 【这是谁】当前项目目录专属的规则覆盖层。
# 【优先级】中——覆盖 global/agents.md，被 append_system.md 覆盖。
# 【怎么用】复制到项目目录（如 config/project/agents.md）后按项目定制；不复制则该项目只有全局规则生效。
# 【生效时机】每次会话启动时读取，改完需重启。

## 场景说明

复制本文件到你的项目目录（如 `config/project/agents.md`），按项目定制。

## 项目上下文（示例）

# 填空即可，Agent 会在本项目的每次会话中带这些背景信息。
# 例：
#   - 项目名：vinf-agent
#   - 项目目标：开源个人认知外延 Agent
#   - 关键约束：纯 stdlib、零依赖、Windows 可跑
- 项目名：
- 项目目标：
- 关键约束：

## 项目级规则

<!-- 在此书写该项目专属的行为规则，示例：

- 代码提交前必须运行测试
- 文档中出现的实验数据必须标注来源
- 对外发布前必须过一遍「落地状态五级标签」检查

-->

# 提示：用 <!-- --> 包住的只是示例说明，正式规则请写在注释之外（或直接删掉示例）。
# 项目级规则优先级高于全局规则，例如全局说「简洁」，项目可以说「本项目的回复要带代码示例」。

## 项目级人设覆盖（可选）

# 只想在本项目内覆盖人设，就填这里；不填则沿用 global/agents.md 的人设。
- 语气：
- 关注点：
